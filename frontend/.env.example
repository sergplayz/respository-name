# Optional in local dev (Vite proxies /api → localhost:8000 when unset).
# Required on Vercel: your Render (or other) API origin, no trailing slash.
# VITE_API_URL=https://your-matcom-api.onrender.com
