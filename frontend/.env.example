# Local dev: omit both → /api uses Vite proxy to localhost:8000.
# Production (Vercel): set RENDER_API_URL or VITE_API_URL in the dashboard (baked in at build).
# RENDER_API_URL=https://your-matcom-api.onrender.com
# VITE_API_URL=https://your-matcom-api.onrender.com
