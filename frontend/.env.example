# Local: Vite proxies /api → http://127.0.0.1:8000 (no env needed).
#
# Vercel (serverless proxy): set any one of these for Production + Preview, then redeploy:
# RENDER_API_URL=https://your-matcom-api.onrender.com
# MATCOM_API_URL=https://your-matcom-api.onrender.com
